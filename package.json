{
  "name": "batflow",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "description": "A static MS-DOS 7.1 control-flow visualizer.",
  "scripts": {
    "check:syntax": "node --check public/app.js && node --check public/lib/source.js && node --check public/lib/batch.js && node --check public/lib/config.js && node --check public/lib/flow.js && node --check public/lib/graph.js",
    "lint": "eslint .",
    "format:check": "prettier --check \"**/*.{css,html,js,json,md,mjs,yml}\"",
    "validate:html": "html-validate public/index.html",
    "test": "node --test tests/*.test.js",
    "test:e2e": "playwright test",
    "check": "npm run check:syntax && npm run lint && npm run format:check && npm run validate:html && npm test",
    "smoke": "node scripts/http-smoke.mjs"
  },
  "engines": {
    "node": ">=24 <25"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@playwright/test": "1.62.0",
    "axe-core": "4.12.1",
    "eslint": "10.8.0",
    "globals": "17.8.0",
    "html-validate": "11.5.6",
    "prettier": "3.9.6"
  }
}
